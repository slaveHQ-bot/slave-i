# Large Feature Workflow

For major features:

DISCOVERY
→ ARCHITECTURE
→ DATA MODEL
→ API/DOMAIN
→ IMPLEMENTATION
→ UI
→ INTEGRATION
→ QA
→ SECURITY
→ PERFORMANCE
→ FINAL VERIFICATION

Create checkpoints.

Do not allow many agents to modify the same foundational files simultaneously.

Main Agent owns sequencing.
