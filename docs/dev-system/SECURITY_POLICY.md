# Security Policy

Agents must never:
- expose secrets
- commit API keys
- bypass security controls
- disable tests to force success
- silently modify permission boundaries
- execute destructive commands without authorization
