# Antigravity Workflow (Two Accounts)

Do not run two independent CTOs against the same working tree.

Recommended model:
- Account A: Main Agent / Implementation
- Account B: Independent review / research / verification

Use separate git worktrees if both need to modify code.
